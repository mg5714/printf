#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdarg.h>
#include <stdlib.h>

void convertToBinary(unsigned int num);
void printPointer(void *ptr);

int _printf(const char *format, ...);

#endif
