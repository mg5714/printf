#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdarg.h>
#include <stdlib.h>

int _putchar(char c)
void convertToBinary(unsigned int num);
void printPointer(void *ptr);

int _printf(const char *format, ...);

#endif
